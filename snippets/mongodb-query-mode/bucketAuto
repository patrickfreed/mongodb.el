# name: bucketAuto
# key: bucketAuto
# --
{
    "$bucketAuto": {
        "groupBy": ${expression},
        "buckets": ${number},
        "output": {
            ${outputs} 
        },
        "granularity": "${string}"
    }
}